$('.modal-title').html("Add New Cohort");

$('.modal-body').html('<%= j render("form") %>');

$('#dialog').modal("show");

$('#dialog').on('shown.bs.modal', function () {
	  $('.first_input').focus();
  });
